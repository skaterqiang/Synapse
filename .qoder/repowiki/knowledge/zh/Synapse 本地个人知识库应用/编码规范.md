- IPC 通道采用 `namespace:action` 命名约定（如 `data:load`、`ai:ask`、`wiki:describe`、`jobs:list`），由 `preload.js` 集中封装为 `kb.*` 方法供渲染层调用。
- 异步流式响应通过事件通道（`ai:chunk`、`wiki:refs`、`jobs:update`）配合 `onXxx` 回调订阅，请求-响应则使用 `invoke`。
- 主进程模块按单一职责拆分至 `src/main/` 下独立文件，由 `main.js` 在应用启动时顺序装配，渲染层不直接访问 Node 模块。